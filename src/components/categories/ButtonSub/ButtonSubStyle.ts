import { StyleSheet } from 'react-native';

const buttonSubStyles = StyleSheet.create({
  title: {
    fontSize: 16,
    fontWeight: '400',
    lineHeight: 21.86,
  },
});

export default buttonSubStyles;
